# 控制面 API 基础路径（控制面默认挂载于 /api/v1 前缀）
# - 本地开发：建议 /api/v1，走下方 Vite 代理，规避跨域
# - 生产：可配为同源反代路径或控制面完整地址（需控制面支持 CORS）
VITE_API_BASE=/api/v1

# 仅本地开发生效：Vite 代理转发目标（控制面地址）
VITE_PROXY_TARGET=http://127.0.0.1:9000
